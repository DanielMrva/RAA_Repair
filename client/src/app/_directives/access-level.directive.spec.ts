import { AccessLevelDirective } from './access-level.directive';

describe('AccessLevelDirective', () => {
  it('should create an instance', () => {
    const directive = new AccessLevelDirective();
    expect(directive).toBeTruthy();
  });
});
